# Starship.rs prompt

## Initial setup

Link configuration:

```sh
ln -s ~/.dotfiles/.config/starship-prompt/starship.toml ~/.config/starship.toml
```

## Themes

The two configuration files only differ in the selected color palette:

```toml
palette = "my-dark-theme"
# vs
palette = "my-light-theme"
```
