# Environment variables

This is probably not the best setup, but it works.

- `~/.config/user-dirs.dirs` should link to `~/.config/environment.d/my-xdg-dirs.conf`, which in turns should link to `~/.dotfiles/.config/environment.d/my-xdg-dirs.conf` (this folder).
